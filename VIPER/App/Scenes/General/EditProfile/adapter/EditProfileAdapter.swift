//
//  EditProfileAdapter.swift
//  VIPER
//
//  Created by Eddy Donald Chinchay Lujan on 31/8/22.
//

import UIKit

class EditProfileAdapter: NSObject {
    
    /*func headerViewTable() -> UIView {
        return UIView(frame: CGRect(x: 0, y: 0, width: view.frame.size.width, height: 60))
    }

    public func createTableHeaderView() -> UIView {
        let header = UIView(
            frame: CGRect(
                x: 0,
                y: 0,
                width: self.view.width,
                height: self.view.height/4).integral)
        
        let size = header.height/1.5
        let profilePhotoButton = UIButton(
            frame: CGRect(
                x: (self.view.width-size)/2,
                y: (header.height-size)/2,
                width: size,
                height: size) )
        header.addSubview(profilePhotoButton)
        
        let label = UILabel(frame: CGRect(
            x: 10,
            y: profilePhotoButton.bottom+5,
            width: view.width-20,
            height: 20 ))
        
    }*/
}
